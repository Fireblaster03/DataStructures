## Objectives
* Implement your own **naive** Binary Search Tree (BST) that is templated.  Provide the following interface:
    * insert(T): only inserts unique values
    * bool contains(T) const
    * size_t size() const
    * void erase(T)
    * for_each(Function fn)
